# WDW trip site (encrypted)

Password-gated page, built with [StatiCrypt](https://github.com/robinmoisson/staticrypt)
from a private planning repo. This repo intentionally contains no trip details — the
`index.html` here is a client-side-encrypted blob; the passphrase to view it is not
stored anywhere in this repo or its history.
